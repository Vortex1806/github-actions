# This is a readme file for github actions demo i will create a yml/yaml file for running the workflow

We have two actions 
1. Greeting the user [only runs on updating the html file]
2. Perform CI and C Delivery
